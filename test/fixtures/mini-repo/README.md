# mini-repo

Fixture repository for Distyl parity tests.

Contains a small auth + parser codebase used to test that the extension
pipeline and CLI pipeline produce identical ranked output for the same
workspace state.
